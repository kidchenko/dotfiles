# Chezmoi ignore file
# Files listed here will not be managed by chezmoi

# Ignore SSH keys if 1Password is not configured or key doesn't exist
{{ if not (and (index . "onepassword") .onepassword) }}
.ssh/id_ed25519
.ssh/id_ed25519.pub
{{ end }}
